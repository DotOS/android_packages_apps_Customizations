<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dot.widgets">

    <application>

        <!-- Widgets -->
        <receiver
            android:name=".DotWidget1"
            android:label="Pill Clock"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dot_widget1_info" />
        </receiver>

        <receiver
            android:name=".DotWidget2"
            android:label="Pill Clock 2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dot_widget2_info" />
        </receiver>

        <receiver
            android:name=".DotWidget3"
            android:label="Diagonal Clock"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dot_widget3_info" />
        </receiver>

        <receiver
            android:name=".DotWidget4"
            android:label="Square Clock"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dot_widget4_info" />
        </receiver>

        <receiver
            android:name=".DotWidget5"
            android:label="Rounded Square Clock"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dot_widget5_info" />
        </receiver>

        <receiver
            android:name=".AndroidClockWidget"
            android:label="Organic Clock"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/android_clock_widget_info" />
        </receiver>
    </application>

</manifest>